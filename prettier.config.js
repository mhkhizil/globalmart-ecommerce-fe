module.exports = {
  semi: true, // Add a semicolon at the end of statements
  singleQuote: true, // Use single quotes instead of double quotes
  trailingComma: 'es5', // Print trailing commas wherever possible in ES5 (objects, arrays, etc.)
  tabWidth: 2, // Specify the number of spaces per indentation level
  useTabs: false, // Indent lines with spaces instead of tabs
  printWidth: 80, // Specify the line length that the printer will wrap
  bracketSpacing: true, // Print spaces between brackets in object literals
  arrowParens: 'avoid', // Avoid parentheses when they are not needed for arrow functions
};
